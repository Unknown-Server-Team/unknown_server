<canvas id="particles" class="particles-bg"></canvas>

<div class="not-found-container animate__animated animate__fadeIn">
    <div class="error-code">
        <span class="digit">4</span>
        <div class="server-icon">
            <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <rect x="2" y="3" width="20" height="18" rx="2" stroke="currentColor" stroke-width="2"/>
                <line x1="2" y1="9" x2="22" y2="9" stroke="currentColor" stroke-width="2"/>
                <line x1="2" y1="15" x2="22" y2="15" stroke="currentColor" stroke-width="2"/>
                <circle cx="6" cy="6" r="1" fill="currentColor"/>
                <circle cx="6" cy="12" r="1" fill="currentColor"/>
                <circle cx="6" cy="18" r="1" fill="currentColor"/>
            </svg>
        </div>
        <span class="digit">4</span>
    </div>
    
    <h2 class="error-message">Page Not Found</h2>
    <p class="error-description">The resource you're looking for might have been moved or doesn't exist.</p>
    
    <div class="action-buttons">
        <a href="/" class="primary-button">Return to Homepage</a>
        <a href="/api-docs" class="secondary-button">View Documentation</a>
    </div>
    
    <div class="tech-details">
        <div class="tech-detail-item">
            <span class="detail-icon">🔍</span>
            <span class="detail-text">Check the URL for typos</span>
        </div>
        <div class="tech-detail-item">
            <span class="detail-icon">📚</span>
            <span class="detail-text">Review our API documentation</span>
        </div>
        <div class="tech-detail-item">
            <span class="detail-icon">⚙️</span>
            <span class="detail-text">Ensure proper authentication</span>
        </div>
    </div>
</div>

<style>
.not-found-container {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 2rem;
    position: relative;
    overflow: hidden;
    color: var(--text-color, #e9e9e9);
}

.error-code {
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
}

.digit {
    font-size: 8rem;
    font-weight: 800;
    background: linear-gradient(45deg, var(--gradient-start, #9b4bff), var(--gradient-end, #e84bff));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    text-shadow: 0 0 10px rgba(155, 75, 255, 0.3);
}

.server-icon {
    width: 6rem;
    height: 6rem;
    color: var(--primary-color, #9b4bff);
    margin: 0 1rem;
    animation: server-pulse 3s ease-in-out infinite;
}

.error-message {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 1rem;
    background: linear-gradient(45deg, var(--gradient-start, #9b4bff), var(--gradient-end, #e84bff));
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
}

.error-description {
    font-size: 1.2rem;
    max-width: 600px;
    margin-bottom: 3rem;
    color: var(--text-muted, #a0a0a0);
}

.action-buttons {
    display: flex;
    gap: 1rem;
    margin-bottom: 3rem;
}

.primary-button, .secondary-button {
    padding: 0.8rem 1.8rem;
    border-radius: 8px;
    font-weight: 600;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
}

.primary-button {
    background: linear-gradient(45deg, var(--gradient-start, #9b4bff), var(--gradient-end, #e84bff));
    color: white;
    box-shadow: 0 4px 15px rgba(155, 75, 255, 0.3);
}

.secondary-button {
    background: transparent;
    border: 2px solid var(--primary-color, #9b4bff);
    color: var(--primary-color, #9b4bff);
}

.primary-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(155, 75, 255, 0.4);
}

.secondary-button:hover {
    background: rgba(155, 75, 255, 0.1);
}

.tech-details {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
    max-width: 400px;
}

.tech-detail-item {
    display: flex;
    align-items: center;
    gap: 0.8rem;
    padding: 0.7rem 1.5rem;
    background: rgba(255, 255, 255, 0.05);
    border-radius: 8px;
    transition: all 0.3s ease;
}

.tech-detail-item:hover {
    background: rgba(155, 75, 255, 0.1);
    transform: translateX(5px);
}

.detail-icon {
    font-size: 1.2rem;
}

.detail-text {
    font-size: 0.9rem;
}

@keyframes server-pulse {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.1); opacity: 0.8; }
}

@media (max-width: 768px) {
    .action-buttons {
        flex-direction: column;
        width: 100%;
        max-width: 300px;
    }
    
    .digit {
        font-size: 5rem;
    }
    
    .server-icon {
        width: 4rem;
        height: 4rem;
    }
    
    .error-message {
        font-size: 2rem;
    }
}
</style>